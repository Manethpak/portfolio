---
import { cn } from "../../helper/cn";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={cn("grid auto-rows-[8rem] gap-4", className)}>
  <slot />
</div>
